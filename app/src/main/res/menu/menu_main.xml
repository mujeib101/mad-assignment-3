<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_refresh"
        android:title="Refresh"
        android:icon="@android:drawable/ic_popup_sync"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_toggle_view"
        android:title="Toggle View Mode"
        app:showAsAction="never" />

    <item
        android:title="Theme">
        <menu>
            <item
                android:id="@+id/action_theme_light"
                android:title="Light Theme" />
            <item
                android:id="@+id/action_theme_dark"
                android:title="Dark Theme" />
            <item
                android:id="@+id/action_theme_custom"
                android:title="Custom Theme" />
        </menu>
    </item>

    <item
        android:id="@+id/action_logout"
        android:title="Logout"
        app:showAsAction="never" />

</menu>
